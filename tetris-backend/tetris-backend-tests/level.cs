using Xunit;
using TetrisBackend;
using System;

namespace TetrisBackendTests
{
    // public class LevelTests
    // {
    //     [Fact]
    //     public void LevelUp()
    //     {
    //         var level = new TetrisLevel();
    //         var total_lines = 1;
    //         for (var expected_level = 0; expected_level < 12; ++expected_level)
    //         {
    //             Assert.Equal(expected_level, level.Level);
    //             var linesForLevel = Math.Min(100, (expected_level + 1)*10);
    //             for (var lines = 1; lines <= linesForLevel; ++lines, ++total_lines)
    //             {
    //                 Assert.Equal(lines == linesForLevel, level.Update(total_lines));
    //             }
    //         }
    //     }
    // }
}
